import React from "react";

import styled from "styled-components";

const LeafContainer = styled.div`

`;

const Leaf: React.FC = () => {
  return (
    <LeafContainer>
    
    </LeafContainer>
  );
};

export default Leaf;
